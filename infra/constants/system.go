package constants

const (
	// AppName is the name of the application.
	AppName = "sheethub"
	// ConfigFile is the name of configuration file.
	ConfigFile = "config.yaml"
	// ConfigPath is the configuration path.
	ConfigPath = AppName + "/" + ConfigFile
	// DefaultDBFile is the default database file.
	DefaultDBFile = "sheethub.sqlite"
	// DefaultDBPath is the default database path.
	DefaultDBPath = AppName + "/" + DefaultDBFile
)
